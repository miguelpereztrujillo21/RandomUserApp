<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RandomUserApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:contentDescription">"No"</item>
    <item name="android:windowTranslucentStatus">true</item>

    </style>

    <style name="Theme.RandomUserApp" parent="Base.Theme.RandomUserApp" >
        <item name="colorPrimary">@color/white</item>
        <item name="android:contentDescription">"No"</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
</resources>